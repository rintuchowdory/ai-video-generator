# Public URL of the FastAPI backend. Use the deployed backend URL in production.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Do not put GROQ_API_KEY, MAGIC_HOUR_API_KEY, or other provider credentials in
# this file. NEXT_PUBLIC_* values are included in the browser bundle.
